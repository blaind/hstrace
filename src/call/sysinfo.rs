use super::prelude::*;

pub(crate) fn get_definitions(_inp: &mut Definitions) {
    /*
    inp.add(
        Ident::sysinfo,
        &["info"],
        &[AV::C_sysinfo(Out)],
        AV::Int(Out),
    ));
    */
}
